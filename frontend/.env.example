# Frontend Environment Variables

# API Base URL - Set this when deploying to production
# For GitHub Pages + Railway setup, use your Railway backend URL
VITE_API_BASE_URL=http://localhost:5001/api

# This file is for documentation. Create .env.local for actual secrets.
# Do NOT commit .env files with real secrets to GitHub.
