# Frontend Production Environment Variables
# The production build is served from the same ASP.NET Core container/origin
# as the API, so no absolute API URL is needed — leave VITE_API_BASE_URL unset
# and the app will call same-origin "/api" paths.
